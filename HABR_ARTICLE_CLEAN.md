# Почему NumPy тормозит на матрицах 16х16 и как микроядро на AVX2 в 100 кб решает эту проблему

Когда профилируешь вычисления или инференс небольших моделей в Python, часто натыкаешься на странную вещь: вроде бы под капотом у NumPy крутится мощный OpenBLAS или Intel MKL, но на небольших матрицах (16x16, 32x32) умножение `A @ B` работает подозрительно медленно — по 3–5 микросекунд на одну операцию.

Если перемножать матрицы 2048x2048 раз в минуту — эти микросекунды незаметны. Но когда у вас токенная авторегрессионная генерация с `batch=1`, фильтр Калмана, физический движок или обработка сенсоров в реальном времени с миллионом вызовов, профайлер показывает, что программа большую часть времени просто простаивает на диспетчеризационном оверхеде библиотек.

Я решил разобраться, откуда берётся этот лаг, и написать предельно компактное решение на C с AVX2 без внешних тяжелых зависимостей, чтобы выжать максимум из одного потока CPU.

---

## Почему гиганты пасуют на мелочи?

OpenBLAS и Intel MKL великолепны на гигантских массивах. Но плата за их универсальность — огромный диспетчеризационный стек:
- Проверка типов, флагов C/Fortran выравнивания и шагов (strides) массивов.
- Логика эвристик: решение о том, стоит ли будить пул потоков OpenMP. Будить потоки ради матрицы 16x16 — это чистый убыток по тактам из-за синхронизации мьютексов и барьеров.
- Лишние аллокации памяти под промежуточные буферы упакованных панелей (packing).

Для матрицы 16x16 из `float32` весь объём данных — это всего:
$$16 \times 16 \times 4 \text{ байта} = 1024 \text{ байта (1 КБ)}$$

Она целиком помещается даже в L1-кэш ядра (32–48 КБ), но NumPy тратит на диспетчеризацию больше времени, чем уходит на сам счёт.

---

## Архитектура микроядра: регистровый тайлинг 6x16

Чтобы вычисления шли с предельной скоростью, процессор вообще не должен ходить в оперативную память во внутреннем цикле. Всё должно крутиться строго в регистрах.

В архитектуре x86-64 с AVX2 доступно 16 векторных регистров `ymm0`–`ymm15` по 256 бит (по 8 чисел `float32` каждый). Я выбрал раскладку микроядра размером 6 строк на 16 столбцов (блок 6x16).

```
         Матрица B (16 столбцов = 2 x YMM)
         [ b0_0 ... b0_7 ] [ b0_8 ... b0_15 ]
                   │                  │
Матрица A          │                  │
(6 строк)          ▼                  ▼
[ a0 ] ──► [ ymm0  ymm1  ]  -> Аккумуляторы строки 0
[ a1 ] ──► [ ymm2  ymm3  ]  -> Аккумуляторы строки 1
[ a2 ] ──► [ ymm4  ymm5  ]  -> Аккумуляторы строки 2
[ a3 ] ──► [ ymm6  ymm7  ]  -> Аккумуляторы строки 3
[ a4 ] ──► [ ymm8  ymm9  ]  -> Аккумуляторы строки 4
[ a5 ] ──► [ ymm10 ymm11 ]  -> Аккумуляторы строки 5
```

**Почему именно 6x16:**
- Чтобы хранить 16 столбцов для 6 строк, нужно ровно **12 регистров-аккумуляторов** (`ymm0`–`ymm11`).
- **2 регистра** (`ymm12`, `ymm13`) нужны под векторную загрузку 16 элементов строки из матрицы B.
- **2 регистра** (`ymm14`, `ymm15`) остаются для бродкаста скалярных элементов матрицы A через `_mm256_set1_ps`.

Итого: заняты все 16 регистров, ни одного лишнего сброса на стек (zero register spilling).

Фрагмент внутреннего цикла вычислений:

```c
// Загружаем 16 float из матрицы B (два YMM-регистра)
__m256 b0 = _mm256_loadu_ps(&B[k * ldb + 0]);
__m256 b1 = _mm256_loadu_ps(&B[k * ldb + 8]);

// Для каждой из 6 строк матрицы A размножаем элемент A[i, k] на весь вектор
// и выполняем аппаратный FMA (Fused Multiply-Add)
__m256 a0 = _mm256_set1_ps(A[0 * lda + k]);
c00 = _mm256_fmadd_ps(a0, b0, c00);
c01 = _mm256_fmadd_ps(a0, b1, c01);

__m256 a1 = _mm256_set1_ps(A[1 * lda + k]);
c10 = _mm256_fmadd_ps(a1, b0, c10);
c11 = _mm256_fmadd_ps(a1, b1, c11);
// ... полностью развёрнуто до 6-й строки
```

Инструкция `_mm256_fmadd_ps` выполняет умножение и сложение за один такт с удвоенной пропускной способностью без промежуточной потери точности. Для матриц большего размера микроядро оборачивается в трехуровневую L1/L2 кэш-блокировку с тайлами $M_c = 64, N_c = 128, K_c = 128$.

---

## Грабли интеграции с Python: Buffer Protocol без копирования

Первый прототип я вызвал через `ctypes`. И тут же наступил на грабли: маршалинг аргументов и валидация типов в `ctypes` съели почти всю сэкономленную скорость.

Поэтому я написал компактный нативный C-extension для CPython через стандартный **Buffer Protocol (`Py_buffer`)**:

```c
Py_buffer viewA, viewB;
if (PyObject_GetBuffer(objA, &viewA, PyBUF_SIMPLE) != 0) return NULL;
if (PyObject_GetBuffer(objB, &viewB, PyBUF_SIMPLE) != 0) {
    PyBuffer_Release(&viewA);
    return NULL;
}

const float* ptrA = (const float*)viewA.buf;
const float* ptrB = (const float*)viewB.buf;
```

Это обеспечивает честный **Zero-Copy**: модуль забирает сырой указатель на непрерывную память прямо из NumPy массива без единой аллокации и сразу передаёт его в микрокернел.

---

## Результаты замеров

- **Тестовый стенд:** x86-64 CPU (Intel AVX2 + FMA), компилятор GCC `-O3 -mavx2 -mfma`.
- **Методология:** Сравнение `A @ B` в NumPy (бэкенд OpenBLAS) и `nanogemm.matmul(A, B)`. Оценка — медиана из 10 000 итераций на одиночном ядре.

| Размер матрицы | NumPy (OpenBLAS) | NanoGEMM (C-ядро) | NanoGEMM (Python API) | Разница |
|:---|:---:|:---:|:---:|:---:|
| **16 × 16** | 3.21 µs | **0.65 µs** | **1.23 µs** | 🚀 **в 2.8 раза быстрее** |
| **32 × 32** | 5.75 µs | **2.18 µs** | **2.74 µs** | 🚀 **в 2.2 раза быстрее** |
| **64 × 64** | 18.70 µs | **16.39 µs** | **17.76 µs** | 🚀 **на 10% быстрее** |

Начиная со 128x128 и выше OpenBLAS уже подключает многопоточность и уходит вперёд, но в диапазоне 1x1 – 64x64 отсутствие диспетчеризационного шума даёт колоссальный выигрыш.

---

## Исходники и воспроизводимость

Библиотека оформлена в компактный пакет **`nanogemm`** (~100 КБ) и опубликована на PyPI:

```bash
pip install nanogemm
# или через uv
uv add nanogemm
```

Использование в коде:

```python
import numpy as np
import nanogemm

a = np.random.randn(16, 16).astype(np.float32)
b = np.random.randn(16, 16).astype(np.float32)

# Быстрое перемножение без накладных расходов BLAS
c = nanogemm.matmul(a, b)
```

- **GitHub репозиторий (MIT):** [github.com/eminsk/nanogemm](https://github.com/eminsk/nanogemm)
- **Интерактивный Google Colab:** [Открыть и протестировать в Colab](https://colab.research.google.com/github/eminsk/nanogemm/blob/main/notebooks/benchmark.ipynb)

Будет интересно увидеть результаты тестов на других архитектурах (особенно AMD Zen 3/4/5 и серверных Xeon/EPYC) — делитесь вашими замерами в комментариях!
